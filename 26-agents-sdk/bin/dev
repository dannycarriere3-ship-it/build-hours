#!/usr/bin/env bash

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

if ! command -v overmind >/dev/null 2>&1; then
  printf 'overmind is required. Install it with: brew install overmind\n' >&2
  exit 1
fi

exec overmind start -f Procfile.dev
